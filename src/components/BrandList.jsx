const BrandList = (props) => {

    return (
        <div>
            {/* {props.brands.map((brand) => (
                <div key={brand.id} className="hello">
            <h1>{brand.title}</h1>
            ))

            } */}
        </div>
    )
}

export default BrandList